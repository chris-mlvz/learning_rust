fn main() {
    // * Creating a New Vector
    // let v: Vec<i32> = Vec::new();
    // let vector = vec![1, 2, 3];

    // * Updating a Vector
    let mut v = Vec::new();
    v.push(5);
    v.push(6);
    v.push(7);
    v.push(8);
    println!("{:?}", v);
}
